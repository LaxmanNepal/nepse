(()=>{const input=document.querySelector('#globalSearch');if(!input)return;const wrap=input.closest('.hero-search')||input.parentElement;if(!wrap)return;wrap.classList.add('nav-stock-search');const menu=document.createElement('div');menu.className='stock-autocomplete';menu.hidden=true;wrap.appendChild(menu);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const stocks=()=>window.NEPSE_STOCKS||window.stocks||[];function show(){const q=input.value.trim().toLowerCase(),a=stocks().filter(s=>!q||(s.name+' '+s.symbol).toLowerCase().includes(q)).slice(0,25);menu.innerHTML=a.length?a.map(s=>`<button class="stock-option" type="button" data-symbol="${esc(s.symbol)}"><span>${esc(s.name)}</span><small>${esc(s.symbol)}</small></button>`).join(''):'<div class="stock-option empty-option">No matching company</div>';menu.hidden=false}function open(sym){location.href=`stock/${encodeURIComponent(String(sym).toLowerCase())}/`}input.addEventListener('focus',show);input.addEventListener('input',show);input.addEventListener('keydown',e=>{if(e.key==='Escape'){menu.hidden=true;input.blur()}if(e.key==='Enter'){const b=menu.querySelector('[data-symbol]');if(b)open(b.dataset.symbol)}});menu.addEventListener('mousedown',e=>{const b=e.target.closest('[data-symbol]');if(b){e.preventDefault();open(b.dataset.symbol)}});document.addEventListener('click',e=>{if(!wrap.contains(e.target))menu.hidden=true});window.addEventListener('nepse-data-ready',show)})();