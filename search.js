(()=>{const input=document.querySelector('#globalSearch'),wrap=document.querySelector('#navSearch');if(!input||!wrap)return;const menu=document.createElement('div');menu.className='stock-autocomplete';menu.hidden=true;wrap.appendChild(menu);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const stocks=()=>window.NEPSE_STOCKS||window.stocks||[];function show(){const q=input.value.trim().toLowerCase();const a=[...stocks()].filter(s=>!q||(`${s.name} ${s.symbol}`).toLowerCase().includes(q)).sort((x,y)=>x.name.localeCompare(y.name));menu.innerHTML=a.length?a.map((s,i)=>`<button class="stock-option" type="button" data-symbol="${esc(s.symbol)}"><span>${esc(s.name)}</span><small>${esc(s.symbol)}</small></button>`).join(''):'<div class="stock-option empty-option">No matching company</div>';menu.hidden=false}function open(sym){location.href=`./stock/${encodeURIComponent(String(sym).toLowerCase())}/`}input.addEventListener('focus',show);input.addEventListener('click',show);input.addEventListener('input',show);input.addEventListener('keydown',e=>{if(e.key==='Escape'){menu.hidden=true;return}if(e.key==='Enter'){const b=menu.querySelector('[data-symbol]');if(b)open(b.dataset.symbol)}if(e.key==='ArrowDown'){e.preventDefault();menu.querySelector('[data-symbol]')?.focus()}});menu.addEventListener('click',e=>{const b=e.target.closest('[data-symbol]');if(b)open(b.dataset.symbol)});document.addEventListener('click',e=>{if(!wrap.contains(e.target))menu.hidden=true});window.addEventListener('nepse-data-ready',show)})();